name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-15]
        python-version: ["3.12", "3.13", "3.14"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Install uv and Python
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --frozen --extra dev --extra export

      - name: Check formatting
        run: uv run --frozen ruff format --check .

      - name: Lint
        run: uv run --frozen ruff check .

      - name: Test
        run: uv run --frozen python -m pytest

      - name: Compile and load custom op
        run: |
          uv run --frozen python - <<'PY'
          import tempfile

          import onnxruntime

          from fast_silero_vad.export.custom_op.build_vad_custom_op import build_vad_custom_op

          onnxruntime.set_default_logger_severity(3)

          with tempfile.TemporaryDirectory() as output_dir:
              library_path = build_vad_custom_op(output_dir)
              options = onnxruntime.SessionOptions()
              options.register_custom_ops_library(library_path)
          PY

      - name: Verify exports against Silero VAD >=6.2.1
        run: |
          uv run --frozen python -m pytest \
            --download-silero-vad \
            tests/test_export_equivalence.py

      - name: Build distributions
        run: uv build
